package cn.kevenpotter.designpatterns.bridge;

/**
 * 概念：将抽象和实现解耦，使得两者可以独立地变化。
 * <p>
 * 优点：
 * 1.抽象和实现分离。这是桥梁模式的主要特征，它完全是为了解决继承的缺点而提出的设计模式。在该模式下，实现可以不受抽象的约束，不用再绑定在一个固定的抽象层次上；
 * 2.优秀的扩充能力；
 * 3.实现细节对客户透明。客户不用关心细节的实现，它已经由抽象层通过聚合关系完成了封装。
 * <p>
 * 桥接模式的本质：分离抽象和实现
 */
public class Overview {
}
